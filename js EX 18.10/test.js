/* function hasSpaces(str){
    let re =/\s/;              // \s = WhiteSpace
    return re.test(str);
}
document.write(hasSpaces("hello world")); */

/* function containsDigit(str){
    let re = /\d/             //  \d = any digit // alternative [0-9]
    return re.test(str);
}
document.write(containsDigit("hello8 world")); */

/* function removeVowels(str){
    let re = /[a,e,i,o,u,y]/g;
    return result = str.replace(re,"" )
}
document.write(removeVowels("take it easy")); */

/* function replaceVowels(str, replacer){
    let re = /[a,e,i,o,u,y]/g;
    return result = str.replace(re, replacer)
}
document.write(replaceVowels("take it easy", "#")); */

/* function lettersOnly(str){
    let re = /[^a-z]/gi;                       // multiple modifier
    return result = str.replace(re, '');
}
document.write(lettersOnly("^,]%4B|@56a![0{2m>b1&4i4"));  */



